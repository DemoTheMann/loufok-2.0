<!DOCTYPE html>
 <html lang="fr">
 
 <head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="/assets/images/logo_pink.ico" type="image/x-icon" id="favicon" >
  <!-- CSS -->
  <link rel="stylesheet" type="text/css" href="../assets/css/style.css" >
  {% block css %}{% endblock %}
  <title>{% block title %}Loufok{% endblock %}</title>
 </head>
 <body>
  {% block content %}{% endblock %}
  
  <footer class="footer">
    <div class="access">
    <img  class="access__icon" tabindex="0" src="/assets/images/access_icon.png" alt="Accessibilité" aria-label="accessibilité" />
      <div class="access__shadow">
        <button class="footer__button access__dys">Dyslexique</button>
        <button class="footer__button access__increase" alt="Augmenter la taille du texte" aria-label="Augmenter la taille du texte" ><img src="/assets/images/increase.svg"/></button>
        <button alt="Diminuer la taille du texte" aria-label="Diminuer la taille du texte" class="footer__button access__decrease" ><img src="/assets/images/decrease.svg"/></button>
      </div>
    </div>
  </footer>
  
  <!-- JS -->
  {% block js %}
  {% endblock -%}
    <script src="/assets/js/access.js"></script>
    <script src="/assets/js/tabNav.js"></script>
    <script>
    //changer la couleur de la favicon en fonction du thème
    window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', function(){
      const isDarkMode = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
      const favicon = document.querySelector('#favicon');
      if (isDarkMode) {
        favicon.href = "/assets/images/logo_white.ico";
      } else {
        favicon.href = "/assets/images/logo_black.ico";
      }
    });
    </script>
 </body>
 </html>